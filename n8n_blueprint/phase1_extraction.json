{
  "name": "Phase 1: Legal AI Extraction",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "extract-pdf",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-extract",
      "name": "Webhook (Extract)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "x-api-key", "value": "={{ $env.ANTHROPIC_API_KEY }}" },
            { "name": "anthropic-version", "value": "2023-06-01" },
            { "name": "content-type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={ "model": "claude-3-5-sonnet-20241022", "max_tokens": 4000, "system": "You are an expert Legal Case Extraction Assistant. Extract structured data from the provided New York State police report (MV-104A) into a JSON object.

INSTRUCTIONS:
1. Identify the CLIENT (injured party) and OPPOSING PARTY.
2. Accident date MUST be YYYY-MM-DD.
3. SOL date is EXACTLY 8 years after accident date.
4. If client is pedestrian, vehicle_info must be null.

SCHEMA:
{
  "client_first_name": "string",
  "client_last_name": "string",
  "client_gender": "male|female|others|unknown",
  "accident_date": "YYYY-MM-DD",
  "accident_time": "string or null",
  "accident_location": "string",
  "accident_description": "string",
  "opposing_party_name": "string",
  "injuries_sustained": "string",
  "number_injured": "integer",
  "vehicle_info": "string or null",
  "vehicle_registration_plate": "string or null",
  "accident_type": "string",
  "statute_of_limitations_date": "YYYY-MM-DD"
}

Return ONLY the raw JSON object. Do not use markdown blocks.", "messages": [ { "role": "user", "content": [ { "type": "document", "source": { "type": "base64", "media_type": "application/pdf", "data": "{{ $binary.data.data }}" } }, { "type": "text", "text": "Extract the case details from this police report." } ] } ] }",
        "options": {}
      },
      "id": "anthropic-extract",
      "name": "Extract Data (Anthropic)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [250, 0]
    },
    {
      "parameters": {
        "jsCode": "try {
  const rawResponse = $input.item.json.content[0].text;
  const extractedData = JSON.parse(rawResponse);
  return { json: extractedData };
} catch (e) {
  throw new Error('Failed to parse AI response: ' + e.message);
}"
      },
      "id": "parse-json",
      "name": "Parse JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 0]
    },
    {
      "parameters": {
        "options": {}
      },
      "id": "respond-webhook",
      "name": "Respond to Vercel",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [750, 0]
    }
  ],
  "connections": {
    "Webhook (Extract)": {
      "main": [ [ { "node": "Extract Data (Anthropic)", "type": "main", "index": 0 } ] ]
    },
    "Extract Data (Anthropic)": {
      "main": [ [ { "node": "Parse JSON", "type": "main", "index": 0 } ] ]
    },
    "Parse JSON": {
      "main": [ [ { "node": "Respond to Vercel", "type": "main", "index": 0 } ] ]
    }
  },
  "active": false,
  "settings": {}
}